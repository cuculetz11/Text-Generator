function retval = sample_n_words (text,  widx, kscvidx, k, stoch, word_set, n)
    % k_secv_corpus = k_secv(text, k);
    % for i = 1:n
    %     probs = sample_next_word(k_secv_corpus, widx, kscvidx, k, stoch);
    %     word = prob_choose(probs, word_set);
    %     display(word);
    %     k_secv_corpus = [k_secv_corpus(2:end), word];
    % endfor
    % retval = k_secv_corpus;
endfunction
