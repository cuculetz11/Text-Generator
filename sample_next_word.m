function probs = sample_next_word (text, words_idx, k_secv_idx, k, stoch)
 
    % k_secv_corpus = k_secv(text, k);
    % probs = zeros(1, length(words_idx.keys));
    % for i = 1:length(words_idx.keys)
    %     probs(i) = 1;
    %     for j = 1:length(k_secv_corpus)
    %         if strcmp(k_secv_corpus{j}, k_secv_idx.keys{i})
    %             probs(i) = probs(i) * stoch(k_secv_idx.values{i}, words_idx.values{j});
    %         endif
    %     endfor
    % endfor

  
endfunction